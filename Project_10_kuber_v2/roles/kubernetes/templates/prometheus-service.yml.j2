apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: {{ k8s_namespace }}
spec:
  type: NodePort
  ports:
    - port: {{ prometheus_port }}
      targetPort: {{ prometheus_port }}
      nodePort: 30090
  selector:
    app: prometheus
