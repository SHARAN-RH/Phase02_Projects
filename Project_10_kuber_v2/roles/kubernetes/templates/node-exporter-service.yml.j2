apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: node-exporter
  ports:
    - name: http-metrics
      port: 9100
      targetPort: 9100
